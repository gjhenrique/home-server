---
# Mealie: Recipe management and meal planning. Behind Caddy with SSO.
# Uses SQLite database (no Postgres needed for single-server deployment).
# See: https://docs.mealie.io/documentation/getting-started/installation/sqlite/

services:
  mealie:
    image: ghcr.io/mealie-recipes/mealie:v3.16.0
    container_name: mealie
    restart: unless-stopped
    ports:
      - "9925:9000"
    deploy:
      resources:
        limits:
          memory: 1000M
    volumes:
      - ${EXT_PATH}/mealie/data:/app/data
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=${TZ:-Europe/Berlin}
      - BASE_URL=https://mealie.${DOMAIN}
      - ALLOW_SIGNUP=false
      - DB_ENGINE=sqlite
      - API_DOCS=false
      # OpenAI/OpenRouter integration for recipe parsing and image analysis
      - OPENAI_API_KEY=${MEALIE_OPENAI_KEY}
      - OPENAI_BASE_URL=${MEALIE_OPENAI_BASE_URL:-https://openrouter.ai/api/v1}
      - OPENAI_MODEL=${MEALIE_OPENAI_MODEL:-openai/gpt-4.1-mini}
      - OPENAI_ENABLE_IMAGE_SERVICES=${MEALIE_OPENAI_ENABLE_IMAGE_SERVICES:-true}
    labels:
      caddy: mealie.${DOMAIN}
      caddy.import: only_tls
      caddy.reverse_proxy: "127.0.0.1:9925"
      homepage.group: Food
      homepage.name: Mealie
      homepage.icon: mealie
      homepage.href: https://mealie.${DOMAIN}
      homepage.description: Recipe management and meal planning
