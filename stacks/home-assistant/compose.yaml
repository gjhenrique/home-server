---
services:
  homeassistant:
    image: ghcr.io/home-assistant/home-assistant:2026.1.2
    restart: unless-stopped
    ports:
      - "127.0.0.1:8123:8123"
    environment:
      - TZ=Europe/Berlin
    volumes:
      - ${EXT_PATH}/homeassistant:/config
      - /run/dbus:/run/dbus:ro
    devices:
      - /dev/ttyUSB0:/dev/ttyUSB0
    labels:
      caddy: ha.${DOMAIN}
      caddy.import: only_tls
      caddy.reverse_proxy: "127.0.0.1:8123"
